{#- Shared fragments — SPEC.md §12. -#}

{%- macro commentary_entry(entry, slug) -%}
<div class="commentary-entry {{ slug }}" id="{{ entry.id }}">
        {%- for paragraph in entry.paragraphs %}
        <p class="commentary-text{% if paragraph.part is not none %} part-{{ paragraph.part }}{% endif %}">{% if loop.first and entry.dibur_hamatchil %}<span class="dibur-hamatchil">{{ entry.dibur_hamatchil }}</span> {% endif %}{{ paragraph.html }}</p>
        {%- endfor %}
      </div>
{%- endmacro -%}
